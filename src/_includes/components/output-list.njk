<section class="output-group">
  <h2>Publications</h2>
  {% set has_publications = false %}
  {% for item in outputs %}
    {% if item.type == "publication" %}
      {% set has_publications = true %}
    {% endif %}
  {% endfor %}
  {% if has_publications %}
  <ul class="output-list">
    {% for item in outputs %}
    {% if item.type == "publication" %}
    <li>
      <p class="output-list__title"><a href="{{ item.url }}" target="_blank" rel="noopener noreferrer">{{ item.title }}</a></p>
      {% if item.authors %}<p class="output-list__meta">{{ item.authors }}{% if item.year %} ({{ item.year }}){% endif %}</p>{% endif %}
      <p class="output-list__description">{{ item.description }}</p>
    </li>
    {% endif %}
    {% endfor %}
  </ul>
  {% else %}
  <p class="output-empty">No publications listed yet.</p>
  {% endif %}
</section>

<section class="output-group">
  <h2>Datasets</h2>
  {% set has_datasets = false %}
  {% for item in outputs %}
    {% if item.type == "dataset" %}
      {% set has_datasets = true %}
    {% endif %}
  {% endfor %}
  {% if has_datasets %}
  <ul class="output-list">
    {% for item in outputs %}
    {% if item.type == "dataset" %}
    <li>
      <p class="output-list__title"><a href="{{ item.url }}" target="_blank" rel="noopener noreferrer">{{ item.title }}</a></p>
      {% if item.authors %}<p class="output-list__meta">{{ item.authors }}{% if item.year %} ({{ item.year }}){% endif %}</p>{% endif %}
      <p class="output-list__description">{{ item.description }}</p>
    </li>
    {% endif %}
    {% endfor %}
  </ul>
  {% else %}
  <p class="output-empty">No datasets listed yet.</p>
  {% endif %}
</section>

<section class="output-group">
  <h2>Software</h2>
  {% set has_software = false %}
  {% for item in outputs %}
    {% if item.type == "software" %}
      {% set has_software = true %}
    {% endif %}
  {% endfor %}
  {% if has_software %}
  <ul class="output-list">
    {% for item in outputs %}
    {% if item.type == "software" %}
    <li>
      <p class="output-list__title"><a href="{{ item.url }}" target="_blank" rel="noopener noreferrer">{{ item.title }}</a></p>
      {% if item.authors %}<p class="output-list__meta">{{ item.authors }}{% if item.year %} ({{ item.year }}){% endif %}</p>{% endif %}
      <p class="output-list__description">{{ item.description }}</p>
    </li>
    {% endif %}
    {% endfor %}
  </ul>
  {% else %}
  <p class="output-empty">No software listed yet.</p>
  {% endif %}
</section>
